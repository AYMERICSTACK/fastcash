import { redirect } from "next/navigation";import { prisma } from "@/lib/prisma";import { getCustomerSession } from "@/lib/session";import { toCatalogProduct } from "@/lib/public-categories";import ProductCard from "@/components/ProductCard";
export const dynamic="force-dynamic";export default async function Page(){const s=await getCustomerSession();if(!s)redirect("/compte/login?next=/compte/favoris");const fav=await prisma.favorite.findMany({where:{customerId:s.customerId},include:{product:{include:{category:true,brand:true,media:{include:{media:true},orderBy:{position:"asc"}}}}},orderBy:{createdAt:"desc"}});return <main className="section"><div className="container"><p className="hero-kicker">Votre sélection</p><h1 className="title-lg">Mes favoris</h1><p className="muted">Retrouvez les pièces que vous souhaitez garder à l'œil.</p>{fav.length?<div className="product-grid">{fav.map(x=><ProductCard key={x.id} product={toCatalogProduct(x.product)}/>)}</div>:<div className="empty"><h2>Aucun favori pour le moment.</h2><p>Ajoutez des produits avec le cœur pour les retrouver ici.</p></div>}</div></main>}
